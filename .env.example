# Local dev (SQLite)
DATABASE_URL="file:./prisma/dev.db"

# OpenAI (opcional). Si no está, el MVP funciona igual (sin respuestas IA).
OPENAI_API_KEY=""
# Modelo sugerido (ej: gpt-5, gpt-5-mini, etc.)
OPENAI_MODEL="gpt-5"
